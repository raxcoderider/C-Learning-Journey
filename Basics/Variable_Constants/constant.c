#include<stdio.h>

int main(){
    int a=32;
    float b= 23.65;
    char c='A';

    // Constant are the value which cannot be changed we say to the constant which value is assigned to the variable

    printf("%d , %f , %c",a,b,c);

    return 0;
}