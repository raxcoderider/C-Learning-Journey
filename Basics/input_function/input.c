#include<stdio.h>

int main(){
    // We uses input function to ask the user to input the value for this we uses scanf 
    int a;
    printf("Please enter the value of a :");
    scanf("%d",&a); 
    // we use & to put the address of the variable

    printf("%d",a);
}